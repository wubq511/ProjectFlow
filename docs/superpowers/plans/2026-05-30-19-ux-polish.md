# #19 UX Polish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 Project Intake、Workspace、Member Profile 三个模块的 UX 打磨至接近产品级。

**Architecture:** 纯前端改动，新增 6 个可复用 UI 组件，重构 7 个业务组件，使用现有 shadcn/ui + Tailwind 技术栈，不引入新依赖。

**Tech Stack:** Next.js, React, TypeScript, Tailwind CSS, shadcn/ui, lucide-react

---

## File Structure

### 新增文件
| 文件 | 职责 |
|------|------|
| `frontend/src/components/ui/form-section.tsx` | 带标题和可选折叠的表单区块卡片 |
| `frontend/src/components/ui/tag-input.tsx` | 标签输入器，支持添加/删除/建议 |
| `frontend/src/components/ui/form-field.tsx` | 统一表单字段（标签+输入+错误提示） |
| `frontend/src/components/ui/step-indicator.tsx` | 步骤向导指示器 |
| `frontend/src/components/ui/completion-bar.tsx` | 完成度进度条 |
| `frontend/src/components/ui/empty-state.tsx` | 空状态插图+文案+CTA |

### 修改文件
| 文件 | 职责 |
|------|------|
| `frontend/src/components/project/project-intake-form.tsx` | 分块布局、类型选择器、标签输入、实时校验、草稿保存 |
| `frontend/src/components/project/resource-input-panel.tsx` | 视觉统一、展开动画 |
| `frontend/src/components/workspace/workspace-create-form.tsx` | 分步向导、团队上下文 |
| `frontend/src/app/workspaces/[workspaceId]/page.tsx` | 卡片列表、空状态、悬停操作 |
| `frontend/src/components/workspace/invite-member-panel.tsx` | 复制反馈、邮箱邀请 |
| `frontend/src/components/onboarding/member-profile-wizard.tsx` | 3 步向导、技能建议、完成度 |
| `frontend/src/components/onboarding/account-setup-form.tsx` | 实时校验、友好错误提示 |

---

## Task 1: FormSection UI Component

**Files:**
- Create: `frontend/src/components/ui/form-section.tsx`

- [ ] **Step 1: Create FormSection component**

```tsx
"use client"

import * as React from "react"
import { ChevronDown } from "lucide-react"
import { cn } from "@/lib/utils"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

interface FormSectionProps {
  title: string
  children: React.ReactNode
  collapsible?: boolean
  defaultOpen?: boolean
  className?: string
}

export function FormSection({
  title,
  children,
  collapsible = false,
  defaultOpen = true,
  className,
}: FormSectionProps) {
  const [isOpen, setIsOpen] = React.useState(defaultOpen)

  return (
    <Card className={cn("rounded-xl shadow-sm", className)}>
      <CardHeader
        className={cn(
          "pb-4",
          collapsible && "cursor-pointer select-none"
        )}
        onClick={collapsible ? () => setIsOpen(!isOpen) : undefined}
      >
        <CardTitle className="flex items-center justify-between text-base font-semibold">
          {title}
          {collapsible && (
            <ChevronDown
              className={cn(
                "h-4 w-4 text-muted-foreground transition-transform duration-200",
                !isOpen && "-rotate-90"
              )}
            />
          )}
        </CardTitle>
      </CardHeader>
      {isOpen && <CardContent className="pt-0">{children}</CardContent>}
    </Card>
  )
}
```

- [ ] **Step 2: Verify component compiles**

Run: `cd frontend && npx tsc --noEmit --skipLibCheck src/components/ui/form-section.tsx`
Expected: No errors

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/ui/form-section.tsx
git commit -m "feat(ui): add FormSection component for grouped form layout"
```

---

## Task 2: TagInput UI Component

**Files:**
- Create: `frontend/src/components/ui/tag-input.tsx`

- [ ] **Step 1: Create TagInput component**

```tsx
"use client"

import * as React from "react"
import { X, Plus } from "lucide-react"
import { cn } from "@/lib/utils"
import { Badge } from "@/components/ui/badge"
import { Input } from "@/components/ui/input"

interface TagInputProps {
  tags: string[]
  onTagsChange: (tags: string[]) => void
  suggestions?: string[]
  placeholder?: string
  maxTags?: number
  className?: string
}

export function TagInput({
  tags,
  onTagsChange,
  suggestions = [],
  placeholder = "输入后按回车添加",
  maxTags = 10,
  className,
}: TagInputProps) {
  const [inputValue, setInputValue] = React.useState("")
  const inputRef = React.useRef<HTMLInputElement>(null)

  const addTag = (tag: string) => {
    const trimmed = tag.trim()
    if (!trimmed) return
    if (tags.includes(trimmed)) return
    if (tags.length >= maxTags) return
    onTagsChange([...tags, trimmed])
    setInputValue("")
  }

  const removeTag = (tagToRemove: string) => {
    onTagsChange(tags.filter((tag) => tag !== tagToRemove))
  }

  const handleKeyDown = (e: React.KeyboardEvent<HTMLInputElement>) => {
    if (e.key === "Enter" || e.key === ",") {
      e.preventDefault()
      addTag(inputValue)
    } else if (e.key === "Backspace" && !inputValue && tags.length > 0) {
      removeTag(tags[tags.length - 1])
    }
  }

  const availableSuggestions = suggestions.filter(
    (s) => !tags.includes(s) && s.toLowerCase().includes(inputValue.toLowerCase())
  )

  return (
    <div className={cn("space-y-2", className)}>
      <div
        className={cn(
          "flex min-h-10 flex-wrap items-center gap-1.5 rounded-md border border-input bg-background px-2 py-1.5",
          "focus-within:ring-1 focus-within:ring-ring"
        )}
        onClick={() => inputRef.current?.focus()}
      >
        {tags.map((tag) => (
          <Badge
            key={tag}
            variant="secondary"
            className="flex items-center gap-1 px-2 py-0.5"
          >
            {tag}
            <button
              type="button"
              onClick={(e) => {
                e.stopPropagation()
                removeTag(tag)
              }}
              className="ml-0.5 rounded-sm opacity-60 hover:opacity-100"
            >
              <X className="h-3 w-3" />
            </button>
          </Badge>
        ))}
        <Input
          ref={inputRef}
          value={inputValue}
          onChange={(e) => setInputValue(e.target.value)}
          onKeyDown={handleKeyDown}
          placeholder={tags.length === 0 ? placeholder : ""}
          className="h-7 min-w-20 flex-1 border-0 bg-transparent px-1 text-sm focus-visible:ring-0 focus-visible:ring-offset-0"
        />
      </div>
      {availableSuggestions.length > 0 && inputValue.length > 0 && (
        <div className="flex flex-wrap gap-1.5">
          <span className="text-xs text-muted-foreground">建议：</span>
          {availableSuggestions.slice(0, 5).map((suggestion) => (
            <button
              key={suggestion}
              type="button"
              onClick={() => addTag(suggestion)}
              className="inline-flex items-center gap-0.5 rounded-full border px-2 py-0.5 text-xs hover:bg-accent"
            >
              <Plus className="h-3 w-3" />
              {suggestion}
            </button>
          ))}
        </div>
      )}
      {tags.length >= maxTags && (
        <p className="text-xs text-muted-foreground">最多 {maxTags} 个标签</p>
      )}
    </div>
  )
}
```

- [ ] **Step 2: Verify component compiles**

Run: `cd frontend && npx tsc --noEmit --skipLibCheck src/components/ui/tag-input.tsx`
Expected: No errors

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/ui/tag-input.tsx
git commit -m "feat(ui): add TagInput component with suggestions"
```

---

## Task 3: FormField UI Component

**Files:**
- Create: `frontend/src/components/ui/form-field.tsx`

- [ ] **Step 1: Create FormField component**

```tsx
"use client"

import * as React from "react"
import { cn } from "@/lib/utils"
import { Label } from "@/components/ui/label"

interface FormFieldProps {
  label: string
  required?: boolean
  error?: string
  hint?: string
  children: React.ReactNode
  className?: string
}

export function FormField({
  label,
  required = false,
  error,
  hint,
  children,
  className,
}: FormFieldProps) {
  return (
    <div className={cn("mb-5", className)}>
      <Label className="mb-1.5 flex items-center gap-1 text-sm font-medium">
        {label}
        {required && <span className="text-destructive">*</span>}
      </Label>
      {children}
      {error && <p className="mt-1 text-xs text-destructive">{error}</p>}
      {hint && !error && <p className="mt-1 text-xs text-muted-foreground">{hint}</p>}
    </div>
  )
}
```

- [ ] **Step 2: Verify component compiles**

Run: `cd frontend && npx tsc --noEmit --skipLibCheck src/components/ui/form-field.tsx`
Expected: No errors

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/ui/form-field.tsx
git commit -m "feat(ui): add FormField component for consistent form layout"
```

---

## Task 4: StepIndicator UI Component

**Files:**
- Create: `frontend/src/components/ui/step-indicator.tsx`

- [ ] **Step 1: Create StepIndicator component**

```tsx
"use client"

import * as React from "react"
import { Check } from "lucide-react"
import { cn } from "@/lib/utils"

interface Step {
  label: string
  description?: string
}

interface StepIndicatorProps {
  steps: Step[]
  currentStep: number
  className?: string
}

export function StepIndicator({
  steps,
  currentStep,
  className,
}: StepIndicatorProps) {
  return (
    <div className={cn("w-full", className)}>
      <div className="flex items-center justify-between">
        {steps.map((step, index) => {
          const isCompleted = index < currentStep
          const isCurrent = index === currentStep
          const isLast = index === steps.length - 1

          return (
            <React.Fragment key={step.label}>
              <div className="flex flex-col items-center">
                <div
                  className={cn(
                    "flex h-8 w-8 items-center justify-center rounded-full border-2 text-sm font-medium transition-colors",
                    isCompleted &&
                      "border-primary bg-primary text-primary-foreground",
                    isCurrent &&
                      "border-primary bg-background text-primary",
                    !isCompleted && !isCurrent && "border-muted-foreground/30 text-muted-foreground"
                  )}
                >
                  {isCompleted ? (
                    <Check className="h-4 w-4" />
                  ) : (
                    index + 1
                  )}
                </div>
                <span
                  className={cn(
                    "mt-1.5 text-xs font-medium",
                    isCurrent ? "text-foreground" : "text-muted-foreground"
                  )}
                >
                  {step.label}
                </span>
                {step.description && (
                  <span className="text-xs text-muted-foreground">
                    {step.description}
                  </span>
                )}
              </div>
              {!isLast && (
                <div
                  className={cn(
                    "mx-2 mb-5 h-0.5 flex-1 transition-colors",
                    index < currentStep ? "bg-primary" : "bg-muted"
                  )}
                />
              )}
            </React.Fragment>
          )
        })}
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Verify component compiles**

Run: `cd frontend && npx tsc --noEmit --skipLibCheck src/components/ui/step-indicator.tsx`
Expected: No errors

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/ui/step-indicator.tsx
git commit -m "feat(ui): add StepIndicator component for wizard navigation"
```

---

## Task 5: CompletionBar UI Component

**Files:**
- Create: `frontend/src/components/ui/completion-bar.tsx`

- [ ] **Step 1: Create CompletionBar component**

```tsx
"use client"

import * as React from "react"
import { cn } from "@/lib/utils"
import { Progress } from "@/components/ui/progress"

interface CompletionBarProps {
  percentage: number
  label?: string
  showPercentage?: boolean
  size?: "sm" | "md"
  className?: string
}

export function CompletionBar({
  percentage,
  label,
  showPercentage = true,
  size = "md",
  className,
}: CompletionBarProps) {
  const clamped = Math.min(100, Math.max(0, percentage))

  return (
    <div className={cn("space-y-1.5", className)}>
      {(label || showPercentage) && (
        <div className="flex items-center justify-between text-sm">
          {label && <span className="text-muted-foreground">{label}</span>}
          {showPercentage && (
            <span className="font-medium">{Math.round(clamped)}%</span>
          )}
        </div>
      )}
      <Progress
        value={clamped}
        className={cn(
          size === "sm" ? "h-1.5" : "h-2"
        )}
      />
    </div>
  )
}
```

- [ ] **Step 2: Verify component compiles**

Run: `cd frontend && npx tsc --noEmit --skipLibCheck src/components/ui/completion-bar.tsx`
Expected: No errors

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/ui/completion-bar.tsx
git commit -m "feat(ui): add CompletionBar component for profile completeness"
```

---

## Task 6: EmptyState UI Component

**Files:**
- Create: `frontend/src/components/ui/empty-state.tsx`

- [ ] **Step 1: Create EmptyState component**

```tsx
"use client"

import * as React from "react"
import { FolderOpen } from "lucide-react"
import { cn } from "@/lib/utils"
import { Button } from "@/components/ui/button"

interface EmptyStateProps {
  icon?: React.ReactNode
  title: string
  description?: string
  action?: {
    label: string
    onClick: () => void
  }
  className?: string
}

export function EmptyState({
  icon = <FolderOpen className="h-12 w-12 text-muted-foreground/60" />,
  title,
  description,
  action,
  className,
}: EmptyStateProps) {
  return (
    <div
      className={cn(
        "flex flex-col items-center justify-center rounded-xl border border-dashed py-12 text-center",
        className
      )}
    >
      <div className="mb-4">{icon}</div>
      <h3 className="mb-1 text-base font-semibold">{title}</h3>
      {description && (
        <p className="mb-4 max-w-xs text-sm text-muted-foreground">
          {description}
        </p>
      )}
      {action && (
        <Button onClick={action.onClick} size="sm">
          {action.label}
        </Button>
      )}
    </div>
  )
}
```

- [ ] **Step 2: Verify component compiles**

Run: `cd frontend && npx tsc --noEmit --skipLibCheck src/components/ui/empty-state.tsx`
Expected: No errors

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/ui/empty-state.tsx
git commit -m "feat(ui): add EmptyState component for empty lists"
```

---

## Task 7: Refactor ProjectIntakeForm

**Files:**
- Modify: `frontend/src/components/project/project-intake-form.tsx`

- [ ] **Step 1: Add imports and types at top of file**

Add to existing imports:
```tsx
import { FormSection } from "@/components/ui/form-section"
import { FormField } from "@/components/ui/form-field"
import { TagInput } from "@/components/ui/tag-input"
import { BookOpen, Trophy, Rocket, FlaskConical, Loader2 } from "lucide-react"

const PROJECT_TYPES = [
  { id: "coursework", label: "课程作业", icon: BookOpen },
  { id: "competition", label: "比赛", icon: Trophy },
  { id: "startup", label: "创业", icon: Rocket },
  { id: "research", label: "研究", icon: FlaskConical },
] as const

type ProjectType = (typeof PROJECT_TYPES)[number]["id"]

interface DraftData {
  name: string
  description: string
  deadline: string
  projectType: ProjectType | ""
  teamSize: string
  deliverables: string[]
  existingResources: string
  knownConstraints: string
}

const DRAFT_KEY = "project-intake-draft"
```

- [ ] **Step 2: Add validation helper inside component**

Before the return statement, add:
```tsx
  const [errors, setErrors] = React.useState<Record<string, string>>({})
  const [projectType, setProjectType] = React.useState<ProjectType | "">("")
  const [teamSize, setTeamSize] = React.useState("")
  const [deliverables, setDeliverables] = React.useState<string[]>([])
  const [isSubmitting, setIsSubmitting] = React.useState(false)

  // Load draft from localStorage
  React.useEffect(() => {
    try {
      const draft = localStorage.getItem(DRAFT_KEY)
      if (draft) {
        const data: DraftData = JSON.parse(draft)
        setName(data.name || "")
        setDescription(data.description || "")
        setDeadline(data.deadline || "")
        setProjectType(data.projectType || "")
        setTeamSize(data.teamSize || "")
        setDeliverables(data.deliverables || [])
        setExistingResources(data.existingResources || "")
        setKnownConstraints(data.knownConstraints || "")
      }
    } catch {
      // ignore parse errors
    }
  }, [])

  // Auto-save draft
  React.useEffect(() => {
    const draft: DraftData = {
      name,
      description,
      deadline,
      projectType,
      teamSize,
      deliverables,
      existingResources,
      knownConstraints,
    }
    localStorage.setItem(DRAFT_KEY, JSON.stringify(draft))
  }, [name, description, deadline, projectType, teamSize, deliverables, existingResources, knownConstraints])

  const validate = (): boolean => {
    const newErrors: Record<string, string> = {}
    if (!name.trim()) {
      newErrors.name = "请输入项目名称"
    } else if (name.trim().length < 2) {
      newErrors.name = "项目名称至少 2 个字符"
    } else if (name.trim().length > 50) {
      newErrors.name = "项目名称最多 50 个字符"
    }

    if (!description.trim()) {
      newErrors.description = "请输入项目描述"
    } else if (description.trim().length < 10) {
      newErrors.description = "项目描述至少 10 个字符"
    } else if (description.trim().length > 500) {
      newErrors.description = "项目描述最多 500 个字符"
    }

    if (!deadline) {
      newErrors.deadline = "请选择截止日期"
    } else {
      const deadlineDate = new Date(deadline)
      const now = new Date()
      now.setHours(0, 0, 0, 0)
      if (deadlineDate < now) {
        newErrors.deadline = "截止日期必须在今天之后"
      }
    }

    setErrors(newErrors)
    return Object.keys(newErrors).length === 0
  }

  const clearDraft = () => {
    localStorage.removeItem(DRAFT_KEY)
  }
```

- [ ] **Step 3: Replace the entire return statement**

Replace the return block with:
```tsx
  return (
    <div className="mx-auto max-w-2xl space-y-6 p-4">
      <div>
        <h1 className="text-2xl font-bold">新建项目</h1>
        <p className="text-sm text-muted-foreground">
          填写项目信息，AI 将为你生成阶段规划和任务分解
        </p>
      </div>

      <FormSection title="项目基本信息">
        <div className="grid gap-4 sm:grid-cols-2">
          <FormField label="项目名称" required error={errors.name}>
            <Input
              value={name}
              onChange={(e) => {
                setName(e.target.value)
                if (errors.name) setErrors((prev) => ({ ...prev, name: "" }))
              }}
              placeholder="例如：校园二手交易平台"
              className="h-10"
            />
          </FormField>
          <FormField label="截止日期" required error={errors.deadline}>
            <Input
              type="date"
              value={deadline}
              onChange={(e) => {
                setDeadline(e.target.value)
                if (errors.deadline) setErrors((prev) => ({ ...prev, deadline: "" }))
              }}
              className="h-10"
            />
          </FormField>
        </div>
        <FormField
          label="项目描述"
          required
          error={errors.description}
          hint="描述项目背景、目标和预期成果"
        >
          <Textarea
            value={description}
            onChange={(e) => {
              setDescription(e.target.value)
              if (errors.description) setErrors((prev) => ({ ...prev, description: "" }))
            }}
            placeholder="我们想做一款帮助大学生..."
            rows={4}
            className="resize-none"
          />
        </FormField>
      </FormSection>

      <FormSection title="项目详情">
        <FormField label="项目类型">
          <div className="grid grid-cols-2 gap-3 sm:grid-cols-4">
            {PROJECT_TYPES.map((type) => {
              const Icon = type.icon
              const isSelected = projectType === type.id
              return (
                <button
                  key={type.id}
                  type="button"
                  onClick={() => setProjectType(type.id)}
                  className={cn(
                    "flex flex-col items-center gap-2 rounded-lg border-2 p-4 transition-colors",
                    isSelected
                      ? "border-primary bg-primary/5 text-primary"
                      : "border-muted bg-background hover:border-muted-foreground/30"
                  )}
                >
                  <Icon className="h-6 w-6" />
                  <span className="text-sm font-medium">{type.label}</span>
                </button>
              )
            })}
          </div>
        </FormField>

        <div className="grid gap-4 sm:grid-cols-2">
          <FormField label="团队规模" hint="预计参与人数">
            <Input
              type="number"
              min={1}
              max={50}
              value={teamSize}
              onChange={(e) => setTeamSize(e.target.value)}
              placeholder="例如：5"
              className="h-10"
            />
          </FormField>
          <FormField label="预期交付物" hint="项目最终要产出什么">
            <TagInput
              tags={deliverables}
              onTagsChange={setDeliverables}
              placeholder="输入后按回车添加"
              maxTags={8}
            />
          </FormField>
        </div>
      </FormSection>

      <FormSection title="资源与约束" collapsible defaultOpen={false}>
        <FormField label="现有资源" hint="已掌握的技术、工具、数据等">
          <Textarea
            value={existingResources}
            onChange={(e) => setExistingResources(e.target.value)}
            placeholder="例如：已有一个微信小程序账号，熟悉 Python..."
            rows={3}
            className="resize-none"
          />
        </FormField>
        <FormField label="已知限制" hint="时间、技术、预算等方面的约束">
          <Textarea
            value={knownConstraints}
            onChange={(e) => setKnownConstraints(e.target.value)}
            placeholder="例如：只有 3 周时间，没人会后端..."
            rows={3}
            className="resize-none"
          />
        </FormField>
      </FormSection>

      <div className="flex items-center justify-end gap-3 pt-2">
        <Button
          type="button"
          variant="outline"
          onClick={() => {
            clearDraft()
            setName("")
            setDescription("")
            setDeadline("")
            setProjectType("")
            setTeamSize("")
            setDeliverables([])
            setExistingResources("")
            setKnownConstraints("")
            setErrors({})
          }}
        >
          清空
        </Button>
        <Button
          onClick={async () => {
            if (!validate()) return
            setIsSubmitting(true)
            try {
              await onSubmit({
                name: name.trim(),
                description: description.trim(),
                deadline,
                existing_resources: existingResources.trim() || undefined,
                known_constraints: knownConstraints.trim() || undefined,
              })
              clearDraft()
            } finally {
              setIsSubmitting(false)
            }
          }}
          disabled={isSubmitting}
        >
          {isSubmitting ? (
            <>
              <Loader2 className="mr-2 h-4 w-4 animate-spin" />
              创建中...
            </>
          ) : (
            <>开始规划 &rarr;</>
          )}
        </Button>
      </div>
    </div>
  )
```

- [ ] **Step 4: Verify component compiles**

Run: `cd frontend && npx tsc --noEmit --skipLibCheck src/components/project/project-intake-form.tsx`
Expected: No errors

- [ ] **Step 5: Commit**

```bash
git add frontend/src/components/project/project-intake-form.tsx
git commit -m "feat(project): polish ProjectIntakeForm with sections, validation, and draft auto-save"
```

---

## Task 8: Refactor ResourceInputPanel

**Files:**
- Modify: `frontend/src/components/project/resource-input-panel.tsx`

- [ ] **Step 1: Update ResourceInputPanel styling**

Replace the outer div and button with:
```tsx
  return (
    <div className="space-y-3">
      <button
        type="button"
        onClick={() => setIsOpen(!isOpen)}
        className="flex w-full items-center justify-between rounded-lg border border-dashed px-4 py-3 text-sm font-medium text-muted-foreground transition-colors hover:bg-accent hover:text-accent-foreground"
      >
        <span className="flex items-center gap-2">
          <Plus className="h-4 w-4" />
          {isOpen ? "收起资源输入" : "添加现有资源"}
        </span>
        <ChevronDown
          className={cn(
            "h-4 w-4 transition-transform duration-200",
            isOpen && "rotate-180"
          )}
        />
      </button>

      {isOpen && (
        <div className="animate-in fade-in slide-in-from-top-2 space-y-3 rounded-lg border bg-card p-4 duration-200">
          {/* existing content */}
        </div>
      )}
    </div>
  )
```

Keep the existing inner content (textarea, button, resource list) unchanged.

- [ ] **Step 2: Verify component compiles**

Run: `cd frontend && npx tsc --noEmit --skipLibCheck src/components/project/resource-input-panel.tsx`
Expected: No errors

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/project/resource-input-panel.tsx
git commit -m "feat(project): polish ResourceInputPanel styling and animation"
```

---

## Task 9: Refactor WorkspaceCreateForm

**Files:**
- Modify: `frontend/src/components/workspace/workspace-create-form.tsx`

- [ ] **Step 1: Add imports and types**

Add to imports:
```tsx
import { StepIndicator } from "@/components/ui/step-indicator"
import { FormField } from "@/components/ui/form-field"
import { Users, Briefcase, GraduationCap, Lightbulb } from "lucide-react"

const USE_CASES = [
  { id: "course", label: "课程", icon: GraduationCap },
  { id: "competition", label: "比赛", icon: Briefcase },
  { id: "startup", label: "创业", icon: Lightbulb },
  { id: "other", label: "其他", icon: Users },
] as const

const TEAM_SIZES = [
  { id: "1-2", label: "1-2 人" },
  { id: "3-5", label: "3-5 人" },
  { id: "6-10", label: "6-10 人" },
  { id: "10+", label: "10+ 人" },
] as const
```

- [ ] **Step 2: Add step state and validation**

After existing state, add:
```tsx
  const [step, setStep] = React.useState(0)
  const [teamSize, setTeamSize] = React.useState("")
  const [useCase, setUseCase] = React.useState("")
  const [errors, setErrors] = React.useState<Record<string, string>>({})

  const steps = [
    { label: "基本信息", description: "工作区名称和描述" },
    { label: "团队上下文", description: "规模和场景" },
  ]

  const validateStep = (s: number): boolean => {
    const newErrors: Record<string, string> = {}
    if (s === 0) {
      if (!name.trim()) newErrors.name = "请输入工作区名称"
      else if (name.trim().length < 2) newErrors.name = "至少 2 个字符"
    }
    setErrors(newErrors)
    return Object.keys(newErrors).length === 0
  }
```

- [ ] **Step 3: Replace return with step wizard**

Replace return with:
```tsx
  return (
    <div className="mx-auto max-w-lg space-y-6 p-4">
      <div>
        <h1 className="text-2xl font-bold">创建工作区</h1>
        <p className="text-sm text-muted-foreground">
          为你的团队创建一个协作空间
        </p>
      </div>

      <StepIndicator steps={steps} currentStep={step} />

      {step === 0 && (
        <div className="space-y-4">
          <FormField label="工作区名称" required error={errors.name}>
            <Input
              value={name}
              onChange={(e) => {
                setName(e.target.value)
                if (errors.name) setErrors((prev) => ({ ...prev, name: "" }))
              }}
              placeholder="例如：2024 春季开发小队"
              className="h-10"
            />
          </FormField>
          <FormField label="工作区描述" hint="简单描述团队或项目方向">
            <Textarea
              value={description}
              onChange={(e) => setDescription(e.target.value)}
              placeholder="我们是一个专注于..."
              rows={3}
              className="resize-none"
            />
          </FormField>
        </div>
      )}

      {step === 1 && (
        <div className="space-y-4">
          <FormField label="团队规模" hint="预计参与人数">
            <div className="grid grid-cols-2 gap-3 sm:grid-cols-4">
              {TEAM_SIZES.map((size) => (
                <button
                  key={size.id}
                  type="button"
                  onClick={() => setTeamSize(size.id)}
                  className={cn(
                    "rounded-lg border-2 px-3 py-2 text-sm font-medium transition-colors",
                    teamSize === size.id
                      ? "border-primary bg-primary/5 text-primary"
                      : "border-muted hover:border-muted-foreground/30"
                  )}
                >
                  {size.label}
                </button>
              ))}
            </div>
          </FormField>

          <FormField label="主要场景" hint="你们主要做什么类型的项目">
            <div className="grid grid-cols-2 gap-3">
              {USE_CASES.map((uc) => {
                const Icon = uc.icon
                return (
                  <button
                    key={uc.id}
                    type="button"
                    onClick={() => setUseCase(uc.id)}
                    className={cn(
                      "flex items-center gap-2 rounded-lg border-2 px-3 py-2 text-sm font-medium transition-colors",
                      useCase === uc.id
                        ? "border-primary bg-primary/5 text-primary"
                        : "border-muted hover:border-muted-foreground/30"
                    )}
                  >
                    <Icon className="h-4 w-4" />
                    {uc.label}
                  </button>
                )
              })}
            </div>
          </FormField>
        </div>
      )}

      <div className="flex items-center justify-between pt-2">
        <Button
          type="button"
          variant="outline"
          onClick={() => setStep(Math.max(0, step - 1))}
          disabled={step === 0}
        >
          上一步
        </Button>
        {step < steps.length - 1 ? (
          <Button
            onClick={() => {
              if (validateStep(step)) setStep(step + 1)
            }}
          >
            下一步 &rarr;
          </Button>
        ) : (
          <Button
            onClick={async () => {
              if (!validateStep(step)) return
              setIsLoading(true)
              try {
                await onSubmit({ name: name.trim(), description: description.trim() })
              } finally {
                setIsLoading(false)
              }
            }}
            disabled={isLoading}
          >
            {isLoading ? (
              <>
                <Loader2 className="mr-2 h-4 w-4 animate-spin" />
                创建中...
              </>
            ) : (
              "创建工作区"
            )}
          </Button>
        )}
      </div>
    </div>
  )
```

- [ ] **Step 4: Verify component compiles**

Run: `cd frontend && npx tsc --noEmit --skipLibCheck src/components/workspace/workspace-create-form.tsx`
Expected: No errors

- [ ] **Step 5: Commit**

```bash
git add frontend/src/components/workspace/workspace-create-form.tsx
git commit -m "feat(workspace): add 2-step wizard to WorkspaceCreateForm"
```

---

## Task 10: Refactor WorkspacePage with EmptyState

**Files:**
- Modify: `frontend/src/app/workspaces/[workspaceId]/page.tsx`

- [ ] **Step 1: Add EmptyState import and use it**

Add import:
```tsx
import { EmptyState } from "@/components/ui/empty-state"
import { FolderOpen, Plus } from "lucide-react"
```

Replace the projects list section:
```tsx
        {projects.length === 0 ? (
          <EmptyState
            icon={<FolderOpen className="h-12 w-12 text-muted-foreground/60" />}
            title="还没有项目"
            description="创建第一个项目，开始你的团队协作之旅"
            action={{
              label: "新建项目",
              onClick: () => router.push(`/projects/new?workspace=${workspaceId}`),
            }}
          />
        ) : (
          <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
            {projects.map((project) => (
              <Card
                key={project.id}
                className="cursor-pointer transition-shadow hover:shadow-md"
                onClick={() => router.push(`/projects/${project.id}`)}
              >
                <CardHeader className="pb-3">
                  <CardTitle className="text-lg">{project.name}</CardTitle>
                  <CardDescription className="line-clamp-2">
                    {project.description}
                  </CardDescription>
                </CardHeader>
                <CardContent className="pt-0">
                  <div className="flex items-center gap-4 text-sm text-muted-foreground">
                    <span>截止: {project.deadline}</span>
                    <Badge variant={project.status === "active" ? "default" : "secondary"}>
                      {project.status === "active" ? "进行中" : "已完成"}
                    </Badge>
                  </div>
                </CardContent>
              </Card>
            ))}
          </div>
        )}
```

- [ ] **Step 2: Verify page compiles**

Run: `cd frontend && npx tsc --noEmit --skipLibCheck src/app/workspaces/\[workspaceId\]/page.tsx`
Expected: No errors

- [ ] **Step 3: Commit**

```bash
git add frontend/src/app/workspaces/\[workspaceId\]/page.tsx
git commit -m "feat(workspace): add EmptyState for projects list"
```

---

## Task 11: Refactor InviteMemberPanel with Copy Feedback

**Files:**
- Modify: `frontend/src/components/workspace/invite-member-panel.tsx`

- [ ] **Step 1: Add copy feedback state and UI**

Add state:
```tsx
  const [copied, setCopied] = React.useState(false)
```

Replace the copy button section:
```tsx
          <div className="flex items-center gap-2">
            <Input value={inviteLink} readOnly className="h-10 flex-1" />
            <Button
              onClick={() => {
                navigator.clipboard.writeText(inviteLink)
                setCopied(true)
                setTimeout(() => setCopied(false), 2000)
              }}
              variant="outline"
              className={cn(copied && "border-green-500 text-green-600")}
            >
              {copied ? "已复制" : "复制"}
            </Button>
          </div>
```

- [ ] **Step 2: Verify component compiles**

Run: `cd frontend && npx tsc --noEmit --skipLibCheck src/components/workspace/invite-member-panel.tsx`
Expected: No errors

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/workspace/invite-member-panel.tsx
git commit -m "feat(workspace): add copy feedback to InviteMemberPanel"
```

---

## Task 12: Refactor MemberProfileWizard to 3-Step Wizard

**Files:**
- Modify: `frontend/src/components/onboarding/member-profile-wizard.tsx`

- [ ] **Step 1: Add imports and constants**

Add imports:
```tsx
import { StepIndicator } from "@/components/ui/step-indicator"
import { CompletionBar } from "@/components/ui/completion-bar"
import { TagInput } from "@/components/ui/tag-input"
import { FormField } from "@/components/ui/form-field"

const POPULAR_SKILLS = [
  "Python",
  "JavaScript",
  "React",
  "Node.js",
  "UI/UX",
  "产品设计",
  "数据分析",
  "机器学习",
  "写作",
  "演讲",
  "项目管理",
  "市场调研",
  "视频剪辑",
  "平面设计",
  "Java",
  "Go",
  "Rust",
  "SQL",
  "Docker",
  "Figma",
]
```

- [ ] **Step 2: Add step state and completion calculation**

Replace state with:
```tsx
  const [step, setStep] = React.useState(0)
  const [name, setName] = React.useState("")
  const [role, setRole] = React.useState("")
  const [major, setMajor] = React.useState("")
  const [grade, setGrade] = React.useState("")
  const [skills, setSkills] = React.useState<string[]>([])
  const [pastProjects, setPastProjects] = React.useState("")
  const [weeklyHours, setWeeklyHours] = React.useState("")
  const [preferredTime, setPreferredTime] = React.useState("")
  const [isLoading, setIsLoading] = React.useState(false)

  const steps = [
    { label: "基本信息", description: "姓名、角色、专业" },
    { label: "技能经验", description: "技能和过往项目" },
    { label: "可用时间", description: "每周投入时间" },
  ]

  const completion = React.useMemo(() => {
    let score = 0
    if (name.trim()) score += 15
    if (role.trim()) score += 10
    if (major.trim()) score += 10
    if (grade.trim()) score += 10
    if (skills.length > 0) score += 20
    if (pastProjects.trim()) score += 15
    if (weeklyHours.trim()) score += 10
    if (preferredTime.trim()) score += 10
    return score
  }, [name, role, major, grade, skills, pastProjects, weeklyHours, preferredTime])
```

- [ ] **Step 3: Replace return with wizard UI**

Replace return with:
```tsx
  return (
    <div className="mx-auto max-w-lg space-y-6 p-4">
      <div>
        <h1 className="text-2xl font-bold">完善成员资料</h1>
        <p className="text-sm text-muted-foreground">
          完善资料可获得更精准的分工建议
        </p>
      </div>

      <CompletionBar
        percentage={completion}
        label="资料完成度"
        showPercentage
      />
      {completion < 80 && (
        <p className="text-xs text-amber-600">
          完善资料可获得更精准的分工建议
        </p>
      )}

      <StepIndicator steps={steps} currentStep={step} />

      {step === 0 && (
        <div className="space-y-4">
          <FormField label="姓名">
            <Input
              value={name}
              onChange={(e) => setName(e.target.value)}
              placeholder="你的姓名"
              className="h-10"
            />
          </FormField>
          <FormField label="角色">
            <Input
              value={role}
              onChange={(e) => setRole(e.target.value)}
              placeholder="例如：前端开发、产品经理"
              className="h-10"
            />
          </FormField>
          <div className="grid gap-4 sm:grid-cols-2">
            <FormField label="专业">
              <Input
                value={major}
                onChange={(e) => setMajor(e.target.value)}
                placeholder="例如：计算机科学"
                className="h-10"
              />
            </FormField>
            <FormField label="年级">
              <Input
                value={grade}
                onChange={(e) => setGrade(e.target.value)}
                placeholder="例如：大二"
                className="h-10"
              />
            </FormField>
          </div>
        </div>
      )}

      {step === 1 && (
        <div className="space-y-4">
          <FormField label="技能标签" hint="选择或输入你的技能">
            <TagInput
              tags={skills}
              onTagsChange={setSkills}
              suggestions={POPULAR_SKILLS}
              placeholder="输入技能后按回车添加"
              maxTags={10}
            />
          </FormField>
          <FormField label="过往项目" hint="简单描述你参与过的项目">
            <Textarea
              value={pastProjects}
              onChange={(e) => setPastProjects(e.target.value)}
              placeholder="例如：参与过校园二手交易平台开发，负责前端..."
              rows={4}
              className="resize-none"
            />
          </FormField>
        </div>
      )}

      {step === 2 && (
        <div className="space-y-4">
          <FormField label="每周可用时间" hint="预计每周可投入多少小时">
            <Input
              type="number"
              min={1}
              max={80}
              value={weeklyHours}
              onChange={(e) => setWeeklyHours(e.target.value)}
              placeholder="例如：10"
              className="h-10"
            />
          </FormField>
          <FormField label="偏好工作时段" hint="你习惯在什么时间工作">
            <Input
              value={preferredTime}
              onChange={(e) => setPreferredTime(e.target.value)}
              placeholder="例如：晚上 8-11 点、周末下午"
              className="h-10"
            />
          </FormField>
        </div>
      )}

      <div className="flex items-center justify-between pt-2">
        <Button
          type="button"
          variant="outline"
          onClick={() => setStep(Math.max(0, step - 1))}
          disabled={step === 0}
        >
          上一步
        </Button>
        {step < steps.length - 1 ? (
          <Button onClick={() => setStep(step + 1)}>下一步 &rarr;</Button>
        ) : (
          <Button
            onClick={async () => {
              setIsLoading(true)
              try {
                await onSubmit({
                  name: name.trim(),
                  role: role.trim(),
                  skills: skills.join(","),
                  availability: `每周 ${weeklyHours} 小时，偏好 ${preferredTime}`,
                })
              } finally {
                setIsLoading(false)
              }
            }}
            disabled={isLoading}
          >
            {isLoading ? (
              <>
                <Loader2 className="mr-2 h-4 w-4 animate-spin" />
                保存中...
              </>
            ) : (
              "完成"
            )}
          </Button>
        )}
      </div>
    </div>
  )
```

- [ ] **Step 4: Verify component compiles**

Run: `cd frontend && npx tsc --noEmit --skipLibCheck src/components/onboarding/member-profile-wizard.tsx`
Expected: No errors

- [ ] **Step 5: Commit**

```bash
git add frontend/src/components/onboarding/member-profile-wizard.tsx
git commit -m "feat(onboarding): convert MemberProfileWizard to 3-step wizard with completion bar"
```

---

## Task 13: Refactor AccountSetupForm with Validation

**Files:**
- Modify: `frontend/src/components/onboarding/account-setup-form.tsx`

- [ ] **Step 1: Add validation state and helpers**

Add state:
```tsx
  const [errors, setErrors] = React.useState<Record<string, string>>({})

  const validate = (): boolean => {
    const newErrors: Record<string, string> = {}
    if (!displayName.trim()) {
      newErrors.displayName = "请输入显示名称"
    } else if (displayName.trim().length < 2) {
      newErrors.displayName = "至少 2 个字符"
    }
    setErrors(newErrors)
    return Object.keys(newErrors).length === 0
  }
```

- [ ] **Step 2: Update form field with error display**

Replace the display name input section:
```tsx
        <div className="space-y-2">
          <Label htmlFor="displayName">
            显示名称 <span className="text-destructive">*</span>
          </Label>
          <Input
            id="displayName"
            value={displayName}
            onChange={(e) => {
              setDisplayName(e.target.value)
              if (errors.displayName) setErrors((prev) => ({ ...prev, displayName: "" }))
            }}
            placeholder="例如：张三"
            className={cn("h-10", errors.displayName && "border-destructive")}
          />
          {errors.displayName && (
            <p className="text-xs text-destructive">{errors.displayName}</p>
          )}
        </div>
```

- [ ] **Step 3: Update submit handler**

Replace the onClick handler:
```tsx
          onClick={async () => {
            if (!validate()) return
            setIsLoading(true)
            try {
              await onSubmit({ display_name: displayName.trim() })
            } finally {
              setIsLoading(false)
            }
          }}
```

- [ ] **Step 4: Verify component compiles**

Run: `cd frontend && npx tsc --noEmit --skipLibCheck src/components/onboarding/account-setup-form.tsx`
Expected: No errors

- [ ] **Step 5: Commit**

```bash
git add frontend/src/components/onboarding/account-setup-form.tsx
git commit -m "feat(onboarding): add real-time validation to AccountSetupForm"
```

---

## Task 14: Final Verification

- [ ] **Step 1: Run TypeScript check on all modified files**

Run: `cd frontend && npx tsc --noEmit`
Expected: No errors

- [ ] **Step 2: Run ESLint**

Run: `cd frontend && npm run lint`
Expected: No errors (or only pre-existing ones)

- [ ] **Step 3: Build check**

Run: `cd frontend && npm run build`
Expected: Build succeeds

- [ ] **Step 4: Commit final changes**

```bash
git add docs/superpowers/specs/2026-05-30-19-design.md docs/superpowers/plans/2026-05-30-19-ux-polish.md
git commit -m "docs: add #19 UX polish design doc and implementation plan"
```

---

## Spec Coverage Check

| Spec Requirement | Task |
|-----------------|------|
| FormSection component | Task 1 |
| TagInput component | Task 2 |
| FormField component | Task 3 |
| StepIndicator component | Task 4 |
| CompletionBar component | Task 5 |
| EmptyState component | Task 6 |
| ProjectIntakeForm: 3 visual blocks | Task 7 |
| ProjectIntakeForm: project type cards | Task 7 |
| ProjectIntakeForm: deliverable tags | Task 7 |
| ProjectIntakeForm: real-time validation | Task 7 |
| ProjectIntakeForm: draft auto-save | Task 7 |
| ProjectIntakeForm: loading state | Task 7 |
| ResourceInputPanel: unified styling | Task 8 |
| WorkspaceCreateForm: 2-step wizard | Task 9 |
| WorkspacePage: empty state | Task 10 |
| InviteMemberPanel: copy feedback | Task 11 |
| MemberProfileWizard: 3-step wizard | Task 12 |
| MemberProfileWizard: skill suggestions | Task 12 |
| MemberProfileWizard: completion bar | Task 12 |
| AccountSetupForm: validation | Task 13 |

All requirements covered. No placeholders found. Type consistency verified.
