"""ProjectMemory warmup and vector initialization package.

Usage:
    python -m app.memory.warmup

Without memory-vector extra: prints skip message and exits 0.
With memory-vector extra: initializes embedding model and exits 0.
On init failure: prints error and exits 1.
"""
